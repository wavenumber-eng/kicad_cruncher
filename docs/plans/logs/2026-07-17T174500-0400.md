+++
type = "plan_log"
id = "2026-07-17T174500-0400"
plan_id = "kicad-cruncher-large-board-profile-api-plan"
step_id = "monkey-followup-backlog"
created = "2026-07-17T174500-0400"
+++

# Log: monkey-followup-backlog

## Monkey Follow-Up Backlog

Recorded deferred upstream/library work in `docs/research/2026-07-17-large-board-profile-followups.md`.

Backlog items:

- `design_json` materialization remains the largest measured `design/dr` stage on the 4-ch public corpus run. It is deferred because the design JSON artifact is part of the command's current public output contract.
- Future work may add an explicit narrower output mode or upstream `kicad-monkey` design JSON/materialization improvements.
- Existing full-output Cruncher commands must not silently replace design JSON, netlisting, mutable board operations, or STEP generation with projection/targeted reads.
- Parser, projection, pull-parser, native tokenizer/parser, and IR/SVG materialization changes remain future `kicad-monkey` work unless Cruncher defines a narrower public command contract first.
