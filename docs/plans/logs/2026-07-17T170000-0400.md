+++
type = "plan_log"
id = "2026-07-17T170000-0400"
plan_id = "kicad-cruncher-large-board-profile-api-plan"
step_id = "independent-research-review"
created = "2026-07-17T170000-0400"
+++

# Log: independent-research-review (reviewer record)

Independent review of the research handoff at `a7be5ad` by the same second
agent that reviewed the kicad-monkey performance and project-API plans.

## Verdict

**Research confirmed; proceed to `implementation-scope-decision`.** The
proposed scope (one Cruncher-local candidate: command-scoped cached PCB review
SVG rendering for `design/dr`; everything else deferred, rejected, or
guidance-only) is endorsed. No missed cheap window was found that outranks the
selected candidate. Fold the decision-step requirements below into the
implementation gates.

## Claims verified against code and environment

- `design/dr` renders one review SVG per copper layer via
  `pcb.to_svg(layers=[copper_layer, "Edge.Cuts"], profile="enriched")` in a
  loop (`kicad_cruncher_cmd_design.py:446-457`). Each call pays a full IR
  build. Confirmed.
- `PcbSvgCompositionRenderCache` exists and is command-scoped
  (`kicad_cruncher_pcb_svg_compositor.py:68`). Confirmed.
- Upstream floor is a real published release: the kicad-monkey remote
  advertises tag `v2026.7.17`, PyPI resolves `kicad-monkey 2026.7.17`, and the
  active environment reports it. The earlier plan-review blocker is resolved.
- Baseline corpus is public: `tests/corpus/kicad/projects/*` fixtures are
  git-tracked in the public repository and documented as redistributable
  copies of the cleaned public kicad-monkey corpus. The public-evidence exit
  criterion is genuinely satisfiable with these boards.
- Baseline arithmetic is self-consistent: post-floor 4-ch design-pcb-review
  stages (2.509 + 9.409 + ~0.9 + 4.887) match the 17.625s total; the PCB
  parse cost lands inside the `design_json` stage via lazy board load.
- The 1.25x-1.39x parse-like improvements match the upstream Monkey full-parse
  expectation (1.3x-1.7x) - a good cross-repo consistency check.
- Harness quality: the probe uses `gc.collect()` before timing,
  `time.perf_counter`, medians over rounds, and records Python and
  kicad-cruncher/kicad-monkey/wn-geometer versions in output JSON. The pinned
  like-for-like harness gate is met.
- Independently reproduced: probe smoke run on taillight passed and reported
  `kicad-monkey 2026.7.17`; `dev-std audit` passed; rack `L99_signoff` passed
  (5/5 files, 25 tests).
- All six findings from the earlier external plan review were incorporated in
  `765b8e5` (pre-floor baseline step + exit criterion, cache invalidation
  gate, pinned harness, blocked-floor contingency, wording fixes).

## Requirements for the decision step (fold in)

1. **Cache-candidate parity must explicitly include viewBox/bbox semantics.**
   Each current per-layer `to_svg` call computes the all-layer bounding box
   before IR build; a cached composition must produce identical viewBox and
   coordinate framing per layer, plus identical enriched-profile metadata,
   styling-pass output, and drill/slot record counts. The candidate log lists
   manifest/styling/drill-slot parity; add bbox/viewBox explicitly.
2. **Scale expectations by copper layer count.** Baselines used
   `--max-copper-layers 2` (~2.4s/layer). The cache benefit grows with layer
   count while `design_json` (9.4s on 4-ch) dominates low-layer boards. Any
   before/after implementation evidence or release-note claim must measure at
   least one all-layer run, not extrapolate from the two-layer probe.
3. **Record the `design_json` cost as deferred-explicit.** It is the largest
   design/dr stage; the deferral (promised artifact; future explicit output
   option or upstream Monkey work) is correct but should be captured in the
   Monkey follow-up backlog step so it is not lost.

## Scope reviewed

Commits `765b8e5` (plan refinement) and `a7be5ad` (inventory, pre-floor
baseline, floor refresh, post-floor baseline, API-fit research, candidate
selection, probe helper, L99 floor update). No implementation, push, tag,
release, publish, or public issue action was performed or authorized by this
review.
