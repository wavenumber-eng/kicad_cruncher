+++
type = "plan_log"
id = "2026-07-17T154500-0400"
plan_id = "kicad-cruncher-large-board-profile-api-plan"
step_id = "pre-floor-headline-baseline"
created = "2026-07-17T154500-0400"
+++

# Log: pre-floor-headline-baseline

## Pre-Floor Headline Baseline

Captured before changing the dependency floor. The active environment and both JSON result files report `kicad-cruncher 2026.7.16`, `kicad-monkey 2026.7.16`, and `wn-geometer 2026.6.10`.

Commands run:

- `uv run python tests/support_scripts/large_board_profile_probe.py --project tests/corpus/kicad/projects/4-ch-backplane/input/4-ch-backplane.kicad_pro --pcb tests/corpus/kicad/projects/4-ch-backplane/input/4-ch-backplane.kicad_pcb --rounds 3 --max-copper-layers 2 --workloads pcb-parse,design-pcb-review,pcb-clean-dry-run,pcb-svg-composition --output rack_results/large_board_profile/pre_floor_2026_7_16_4ch.json`
- `uv run python tests/support_scripts/large_board_profile_probe.py --project tests/corpus/kicad/projects/speedy_processing_module/input/11-10084__speedy_processing_module__B.kicad_pro --pcb tests/corpus/kicad/projects/speedy_processing_module/input/11-10084__speedy_processing_module__B.kicad_pcb --rounds 3 --max-copper-layers 2 --workloads pcb-parse,design-pcb-review,pcb-clean-dry-run,pcb-svg-composition --output rack_results/large_board_profile/pre_floor_2026_7_16_speedy.json`

Results, median seconds across three rounds:

| Board | Workload | Median seconds | Notable stage medians |
| --- | ---: | ---: | --- |
| 4-ch-backplane | `pcb-parse` | 10.384 | Full `KiCadPcb.from_file()` parse. |
| 4-ch-backplane | `pcb-clean-dry-run` | 10.935 | Dry-run inventory over full mutable PCB model. |
| 4-ch-backplane | `design-pcb-review` | 22.638 | `load_design` 4.079s; `design_json` 11.861s; `kicad_netlist` 0.959s; two-layer PCB SVG loop 5.059s. |
| 4-ch-backplane | `pcb-svg-composition` | 21.479 | `load_pcb` 15.588s; cached two-layer composition 5.394s. |
| Speedy Processing Module | `pcb-parse` | 7.487 | Full `KiCadPcb.from_file()` parse. |
| Speedy Processing Module | `pcb-clean-dry-run` | 7.499 | Dry-run inventory over full mutable PCB model. |
| Speedy Processing Module | `design-pcb-review` | 17.403 | `load_design` 3.410s; `design_json` 8.643s; `kicad_netlist` 0.644s; two-layer PCB SVG loop 4.443s. |
| Speedy Processing Module | `pcb-svg-composition` | 10.719 | `load_pcb` 7.704s; cached two-layer composition 2.861s. |

Interpretation before repin:

- The dependency-refresh comparison is now capturable without relying on deleted Monkey-plan artifacts.
- `design/dr` has two visible local costs: broad design JSON materialization and repeated PCB review SVG rendering. The two-layer SVG loop is large enough to evaluate a command-scoped cache candidate, but not the only bottleneck.
- `pcb clean --dry-run` is essentially full PCB parse cost plus a small inventory pass, so projection is unlikely to preserve mutation/dry-run semantics unless a separate early-exit gate is discovered.
- `pcb-svg` already uses a command-scoped render cache. Its total time is dominated by full PCB load plus configured layer composition, so any Cruncher-local optimization needs evidence beyond merely adding another cache.
