+++
type = "plan_log"
id = "2026-07-17T153500-0400"
plan_id = "kicad-cruncher-large-board-profile-api-plan"
step_id = "command-surface-inventory"
created = "2026-07-17T153500-0400"
+++

# Log: command-surface-inventory

## Command Surface Inventory

Current dependency state before any repin:

- `uv.lock` resolves `kicad-monkey 2026.7.16` from PyPI.
- The active environment reports `kicad-monkey 2026.7.16`.
- `pyproject.toml` still declares `kicad-monkey>=2026.7.16`.

Inventory and cost model:

| Surface | Owning command/module | Board/project API use | Initial cost model | API-fit note |
| --- | --- | --- | --- | --- |
| `design`, `design-review`, `dr` | `kicad_cruncher_cmd_design.py` | `KiCadDesign.from_file()`, `design.to_json()`, `to_netlist_json()`, `to_kicad_netlist_sexpr()`, schematic IR/SVG, then `design.pcb.to_svg()` once per copper layer | Broad/full design load plus repeated PCB IR/SVG render setup per copper layer | Highest local candidate: replace the per-layer `pcb.to_svg()` loop with command-scoped cached IR/SVG composition if output parity holds. |
| `megamaid` | `kicad_cruncher_cmd_megamaid.py` plus shared design review bundle | Calls the same design-review bundle builder | Inherits `design/dr` costs when design review is enabled | Improvements in shared design-review code should flow through. |
| `pcb-svg` | `kicad_cruncher_cmd_pcb_svg.py`, `kicad_cruncher_pcb_svg_compositor.py` | `KiCadPcb.from_file()`, then `PcbSvgCompositionRenderCache` for base IR/root SVG, physical fragments, holes, board regions | Full board load; already has command-scoped cache for configured views/layers; virtual/HLR layers can add Geometer/model costs | Confirm no unnecessary family/render work for selected views; cache should remain command-scoped unless invalidation is designed and tested. |
| `pcb clean`, daemon/plugin cleanup | `kicad_cruncher_cmd_pcb.py`, `kicad_cruncher_pcb_clean.py`, `kicad_cruncher_daemon.py` | `KiCadPcb(...)` for dry-run report, apply, and IPC mutation request | Full mutable board model; scans layers, footprint graphics, board graphics, generated items, value fields | Projection is unlikely to replace apply/mutation safety. Look for cheap early exits/config gating only. Long-lived daemon must not share stale render/board caches. |
| `pcb-layer-step` | `kicad_cruncher_cmd_pcb_layer_step.py`, `kicad_cruncher_pcb_layer_step.py` | `KiCadPcb.from_file()`, feature extraction, board-region classification, Geometer planar STEP request | Full board geometry load plus selected feature extraction and optional fusion/export costs | Projection/targeted reads may help only if selected features avoid broad geometry; otherwise full model is correct. |
| `bom` | `kicad_cruncher_cmd_bom.py`, `kicad_manufacturing_design.py` | `KiCadManufacturingDesign.from_file()` -> `KiCadDesign.from_file()`, `assemble(...)` | Schematic-centered but may load PCB for variant/footprint data when project/PCB is present | Evaluate whether schematic-only BOM modes can avoid board load; project modes may need full design. |
| `pnp` | `kicad_cruncher_cmd_pnp.py`, `kicad_manufacturing_design.py` | `KiCadDesign.from_file()`, `design.pcb`, raw footprint scan keyed by reference | Needs PCB placement data and aux-axis origin | Full model likely correct; possible lookup/index micro-wins only. |
| `jlc` | `kicad_cruncher_cmd_jlc.py`, shared BOM/PnP model | Loads manufacturing design, emits BOM + CPL | Combines BOM and PnP costs | Follows BOM/PnP decisions. |
| `health` | `kicad_cruncher_cmd_health.py` | Filesystem/project asset scan, no KiCad Monkey board parse found in first pass | Directory traversal and path counting, not PCB parse/render | Not a large-board parser candidate. |
| `project`, `project-lib`, `lib-extract`, `schematic`, `plugin`, `kicad`, `version` | Respective command modules | No large-board PCB parse/render loop identified in first pass, except plugin/daemon uses cleanup endpoint indirectly | Mostly filesystem/config/KiCad process work | Out of main profiling scope unless later evidence says otherwise. |

Hotspots to measure first:

1. `design/dr` on a public large project: measure full bundle and break down design load/json/netlist/schematic SVG/PCB SVG loop if feasible.
2. `pcb-svg` on the same board/project with default or focused config: verify current cache behavior and whether selected views still trigger broad work.
3. `pcb clean --dry-run`: confirm full-model cost and whether config gates avoid disabled families.
4. `pcb-layer-step`: measure only after confirming whether Geometer availability makes the harness stable on this machine.

Initial candidate ranking before timing:

1. `design/dr` cached PCB review rendering: likely cheapest Cruncher-local win because `pcb-svg` already solved the pattern with `PcbSvgCompositionRenderCache` while `design/dr` still calls `pcb.to_svg()` per copper layer.
2. Documentation/guidance updates: clarify which commands are intentionally full-model and which are narrow/config-sensitive.
3. `pcb-svg` selected-view/family gating: worth verifying; may already be acceptable.
4. `pcb clean` and `pcb-layer-step`: likely full-model by contract; only accept implementation if timing shows a low-risk local gate.

No implementation started.
